/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["image.tmdb.org"], // Replace with the domain you are using
  },
  // ... other configurations ...
};

module.exports = nextConfig;
